{
  "columns": [
    {
      "name": "transOrigType",
      "type": "Type",
      "nullable": false,
      "nullRate": 75,
      "samples" : ["Invoice"],
      "options": ["Evaluated receipts settlement","Invoice"],
      "queryCol": "(CASE WHEN BUSTRANS.busTransTypeName='Evaluated Receipts Settlement' THEN 'Evaluated Receipts Settlement' WHEN BUSTRANS.busTransTypeName='Invoice' AND TRANSPER.partyRoleTypeName='Seller' THEN 'Invoice' WHEN BUSTRANS.busTransTypeName='Asset Movement' AND TRANSPER.partyRoleTypeName='Owner' THEN 'Invoice' ELSE null END)"
    }
  ]
}
